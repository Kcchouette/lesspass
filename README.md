# LessPass HTML pure

## How to build

`yarn build` using the branch [lesspass-pure](https://github.com/Kcchouette/lesspass/tree/lesspass-pure)  
And then, I change
- in the `assets/index-....js` file, `/locales` → `locales`
- in the `index.html` file, `"/` → `"`
